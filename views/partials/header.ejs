<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow fixed-top">
  <a class="nav-brand" href="/"><img src="../images/logo.svg" height="40px" width="80px"
    alt=""> <span class="sr-only">(current)</span></a>  
    <div class="nav-brand">
      <a class="nav-link ml-2" href="/shopping-cart" tabindex="-2"><i class="fas fa-shopping-cart mr-2"></i>Giỏ hàng
        <% try {   %>
        <span class="badge badge-primary" id="totalQty"><%= session.cart.totalQty %></span>
        <% } catch (error) { %>
        <span>0</span>
        <% } %>
      </a>
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo03"
      aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
      <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
        <li class="nav-item active">
        </li>
        <% if(login) {%>
        <li class="nav-item">
            <% role %>
          <% if(role === 'admin') { %>
          <a class="nav-link" href="/admin">Quản lý</a>
          <% }else if(role === 'manager') { %>
            <a class="nav-link" href="/admin">Quản lý</a>
          <% }else if(role === 'staff') { %>
            <a class="nav-link" href="/admin">Công việc</a>
            <% }else { %>
          <a class="nav-link" href="/users/profile">Hồ sơ</a>
          <% } %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/users/logout" tabindex="-1">Đăng xuất</a>
        </li>
        <% } else { %>
        <li class="nav-item">
          <a class="nav-link" href="/users/login">Đăng nhập</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/users/register" tabindex="-1">Tạo tài khoản</a>
        </li>
        <% } %>
  
        
      </ul>
      <form class="form-inline my-2 my-lg-0" action="/search" method="get">
        <input class="form-control mr-sm-2" id="formSearch" name="p" type="search" placeholder="Chuột, bàn phím, v..v.."
          aria-label="Search" required/>
        <button class="btn btn-outline-success my-2 my-sm-0" id="btnSearch" type="submit">
          Tìm kiếm
        </button>
      </form>
    </div>
  </nav>
  <!-- <div class="progress" id="pr">
    <div class="progress-bar bg-danger fixed-top" role="progressbar" style="width: 100%; height: 3px;" aria-valuenow="100"
      aria-valuemin="0" aria-valuemax="100"></div>
  </div> -->